shared:
    image: node:8

jobs:
    main:
        requires: [~pr, ~commit]
        steps:
            - meta: |
                meta set tests.coverage 100
                meta set tests.results 'alert("Hello! I am an alert box!!")'
#             - install: npm install
#             - test: npm test
